<div id="welcome">
  <div class="container">
    <h1>List BriefCases</h1>
    <hr/>
    <div class="buttons">
      <!--<a class="btn btn-info" href="/account/profile">Update my email</a>-->
      <button class="btn btn-outline-info ml-2" @click="clickOpenDashboardModalButton()">New Briefcase</button>
    </div>
  </div>
  <div class="container">
  	<div style="text-align: center;margin-top: 20px;" class="col-md-12">
		<table id="Maletines" class="table table-striped table-bordered" style="width:100%">
	        <thead>
	            <tr>
	                <th>Name</th>
                  <th>Status</th>
                  <th>Options</th>
	            </tr>
	        </thead>
	        <tbody>
	        	<% _.each(Maletines, function (Maletin) { %>
	    		<tr>
	                <td><%= Maletin.name %></td>
                  <td><%= Maletin.status %></td>
                  <th>
                     <a href="/api/Maletin/delete/<%= Maletin.id %>" class="btn btn-outline-danger ml-2" @click="">Delete</a>
                    <a href="/api/Maletin/<%= Maletin.id %>" class="btn btn-outline-primary ml-2" @click="">Update</a>
                  </th>
	            </tr>
	  			<% }) %>
	            
	        </tbody>
    	</table>
	</div>
  </div>
  <modal class="example-modal" v-if="dashboardModalVisible" @close="closeDashboardModal()" v-cloak>
    <div class="modal-header">
      <h2 class="modal-title">New Briefcase</h2>
       <hr/>
      <button type="button" class="modal-close-button" data-dismiss="modal" aria-label="Close"></button>
    </div>
    <div class="modal-body">
      <h5 class="section-heading">Form Briefcase</h5>
      <form method="POST" action="/api/Maletin/create">
	      <div class="modal-body mx-3">
	            <div class="md-form mb-5">
	               <label data-error="wrong" data-success="right" for="defaultForm-email">Name</label>
	                  <i class="fa fa-briefcase prefix grey-text"></i>
	                <input type="text" id="name" name="name" class="form-control validate" placeholder="@example" required="true">  
	            </div>

	      </div>
          <div class="d-flex justify-content-center">
              <button type="input" class="btn btn-info" value="Create"> Save</button>
          </div>
      </form>
    </div>
    <div class="modal-footer flex-row-reverse justify-content-between">
      <button type="button" data-dismiss="modal" class="btn btn-outline-info">Close</button>
    </div>
  </modal>


</div>
<%- /* Expose locals as `window.SAILS_LOCALS` :: */ exposeLocalsToBrowser() %>